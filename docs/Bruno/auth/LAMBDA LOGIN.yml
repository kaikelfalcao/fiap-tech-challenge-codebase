info:
  name: LAMBDA LOGIN
  type: http
  seq: 1

http:
  method: POST
  url: "{{LAMBDA_URL}}"
  body:
    type: json
    data: |-
      {
        "cpf": "293.085.420-00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
