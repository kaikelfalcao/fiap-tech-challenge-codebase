info:
  name: LIST
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/catalog/services"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
