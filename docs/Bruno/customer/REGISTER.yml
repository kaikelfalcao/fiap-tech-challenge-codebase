info:
  name: REGISTER
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/customers"
  body:
    type: json
    data: |-
      {
          "taxId": "529.982.247-25",
          "fullName": "John Doe",
          "phone": "(71) 99999-0000",
          "email": "john@doe.com"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
