info:
  name: LIVENESS
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/health/liveness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
