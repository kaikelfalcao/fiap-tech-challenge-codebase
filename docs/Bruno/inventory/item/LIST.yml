info:
  name: LIST
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/inventory/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
