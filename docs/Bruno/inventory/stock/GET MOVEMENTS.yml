info:
  name: GET MOVEMENTS
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/inventory/items/:id/stock/movements"
  params:
    - name: id
      value: 49db3fcd-f8d1-4c39-b664-9119cd944227
      type: path
  body:
    type: json
    data: |-
      {
        "newQuantity": 100,
        "note": "Inventário anual — recontagem física"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
