apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-service
spec:
  type: NodePort
  selector:
    app: tech-challenge-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
